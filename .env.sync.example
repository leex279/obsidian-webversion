# Vault Sync Service Configuration
#
# This file configures optional vault synchronization services.
# Services are enabled by running additional Docker Compose files:
#
#   Syncthing:  docker-compose -f docker-compose.yml -f docker-compose.sync-syncthing.yml up -d
#   rclone:     docker-compose -f docker-compose.yml -f docker-compose.sync-rclone.yml up -d
#   Both:       docker-compose -f docker-compose.yml -f docker-compose.sync-syncthing.yml -f docker-compose.sync-rclone.yml up -d

# ============================================================================
# SHARED SYNC CONFIGURATION
# ============================================================================

# User and Group Configuration
# MUST match the values in your main .env file to avoid permission conflicts
# Run `id $USER` to get your uid/gid
PUID=1000
PGID=1000

# Timezone Configuration
# Should match Obsidian container timezone for consistent file timestamps
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Etc/UTC

# ============================================================================
# SYNCTHING CONFIGURATION (Peer-to-Peer Sync)
# ============================================================================

# No additional environment variables required for Syncthing
# Configuration is done through the Syncthing web GUI at http://localhost:8384
#
# Setup Steps:
#   1. Access GUI: http://localhost:8384
#   2. Set username/password: Actions → Settings → GUI → Authentication
#   3. Add remote devices: Actions → Show ID (share this with other devices)
#   4. Configure folders: Add /vaults folder and share with remote devices
#
# Ports Used:
#   8384/tcp:  Web GUI
#   22000:     File transfers (TCP + UDP)
#   21027/udp: Local discovery
#
# Documentation: docs/sync-syncthing-guide.md

# ============================================================================
# RCLONE CONFIGURATION (Cloud Provider Sync)
# ============================================================================

# rclone Remote Name
# The name of the configured remote from rclone.conf
# Example: gdrive, dropbox, onedrive
# Run `docker-compose -f docker-compose.sync-rclone.yml run --rm rclone config` to create
RCLONE_REMOTE=gdrive

# rclone Remote Path
# Path within the cloud storage to sync
# Example: /ObsidianVaults or / for root
RCLONE_REMOTE_PATH=/vaults

# NOTE: rclone configuration requires interactive setup
# See docs/sync-rclone-guide.md for provider-specific instructions:
#   - Google Drive OAuth setup
#   - Dropbox API token generation
#   - OneDrive authentication flow
#   - Performance tuning per provider
