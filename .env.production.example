# Production Environment Configuration for Obsidian Remote with Caddy SSL

# ============================================================================
# CADDY REVERSE PROXY CONFIGURATION
# ============================================================================

# Domain Configuration
# Your custom domain name (without http:// or https://)
# DNS A/AAAA record MUST point to your server's public IP before starting
# Example: obsidian.example.com or notes.yourdomain.com
DOMAIN=obsidian.example.com

# Let's Encrypt Email
# Valid email address for Let's Encrypt certificate registration and renewal notifications
# Example: admin@example.com
CADDY_EMAIL=admin@example.com

# ============================================================================
# OBSIDIAN REMOTE CONFIGURATION
# ============================================================================

# User and Group Configuration
# Run `id $USER` to get your uid/gid
PUID=1000
PGID=1000

# Timezone Configuration
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Etc/UTC

# Web Interface Ports (Internal - proxied through Caddy)
# These ports are NOT exposed externally, only accessible via Caddy reverse proxy
CUSTOM_PORT=8080
CUSTOM_HTTPS_PORT=8443

# Authentication (Optional)
# Leave empty to disable authentication at application level
# Note: Caddy handles SSL/TLS, this is for application-level auth only
CUSTOM_USER=
PASSWORD=

# Reverse Proxy Configuration
# Leave empty - Caddy handles proxying automatically
SUBFOLDER=

# Interface Customization
TITLE=Obsidian Remote
FM_HOME=/vaults

# Docker Mods (Optional)
# Enable git support: DOCKER_MODS=linuxserver/mods:universal-git
# Enable custom packages: DOCKER_MODS=linuxserver/mods:universal-package-install
# Multiple mods: DOCKER_MODS=linuxserver/mods:universal-git|linuxserver/mods:universal-package-install
DOCKER_MODS=

# Package Installation (Optional)
# Requires: DOCKER_MODS=linuxserver/mods:universal-package-install
# Example for CJK support: INSTALL_PACKAGES=fonts-noto-cjk fonts-noto-extra
INSTALL_PACKAGES=

# Keyboard Layout (Optional)
# Examples: en-us-qwerty, de-de-qwertz, fr-fr-azerty
# See: https://github.com/linuxserver/docker-digikam#keyboard-layouts
KEYBOARD=
